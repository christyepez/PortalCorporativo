# Portal CRM Navigation Contract

## P4 Status

CrmNavigationReadiness: PendingPortalConsumerContract.

## Intended Module Metadata

- Module code: `crm`.
- Route prefix: `/crm` placeholder only.
- Resource namespace: `crm.*` placeholder only.
- Permission namespace: `crm.*` placeholder only.

## Rules

CRM must not duplicate Portal Auth/Menu ownership, must not share Portal databases and must not activate productive navigation through P4. Concrete routes and permissions require a later consumer onboarding gate.
